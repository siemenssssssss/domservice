{% extends 'base.html' %}
{% block title %}Отзывы жильцов{% endblock %}
{% block content %}
<div class="container mt-4">
    <h2 class="mb-4"><i class="fas fa-star"></i> Отзывы о работе УК "ДомСервис"</h2>
    
    <div class="row mb-4">
        <div class="col-md-4">
            <div class="card bg-primary text-white text-center">
                <div class="card-body">
                    <h3 class="display-4">{{ reviews_count }}</h3>
                    <p>всего отзывов</p>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card bg-success text-white text-center">
                <div class="card-body">
                    <h3 class="display-4">{{ avg_rating|floatformat:1 }}</h3>
                    <p>средний рейтинг</p>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card bg-warning text-dark text-center">
                <div class="card-body">
                    <h3 class="display-4">98%</h3>
                    <p>довольных жильцов</p>
                </div>
            </div>
        </div>
    </div>
    
    <div class="row">
        {% for review in reviews %}
        <div class="col-md-6 mb-3">
            <div class="card">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center mb-2">
                        <div class="stars">
                            {% for i in "12345"|make_list %}
                                {% if forloop.counter <= review.rating %}
                                    <i class="fas fa-star text-warning"></i>
                                {% else %}
                                    <i class="far fa-star text-warning"></i>
                                {% endif %}
                            {% endfor %}
                        </div>
                        <small class="text-muted">{{ review.date|default:review.created_at|date:"d.m.Y" }}</small>
                    </div>
                    <p class="mb-2">{{ review.comment }}</p>
                    <small class="text-muted">— {{ review.name|default:review.request.user.get_full_name|default:review.request.user.username }}</small>
                </div>
            </div>
        </div>
        {% endfor %}
    </div>
</div>

<style>
    .stars i {
        font-size: 1rem;
        margin-right: 2px;
    }
</style>
{% endblock %}